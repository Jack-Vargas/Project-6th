using System.Collections;
using System.Collections.Generic;
using System.Security.Cryptography;
using UnityEngine;

public class SmartRandoLocalAdjust : MonoBehaviour
{
    public float randoNum;
    public float minVariability;

    // Update is called once per frame
    void Update()
    {
       // transform.position.x = RandomNum


       
        // if randonum is greater than gameobject position + minVariability
        //OR
        //Make the max and min of the rando num 5 less than you want it then set the minVar to 5
    }
}
